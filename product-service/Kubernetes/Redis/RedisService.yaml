apiVersion: v1
kind: Service
metadata:
  name: redis-service
  labels:
    app: redis
    microservice: product-service
spec:
  selector:
    app: redis
    microservice: product-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: NodePort
  
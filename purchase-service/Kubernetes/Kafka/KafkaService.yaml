apiVersion: v1
kind: Service
metadata:
  name: kafka-service
  labels:
    app: kafka
    microservice: purchase-service
spec:
  selector:
    app: kafka
    microservice: purchase-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: NodePort
  
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Storage
    password: klochko_08
    username: postgres
    driver-class-name: org.postgresql.Driver

  cache:
    type: redis
  data:
    redis:
      host: localhost
      port: 6379


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8084